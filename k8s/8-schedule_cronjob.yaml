apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-name
spec:
  schedule: "*/7 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
          - name: cron-container-name
            image: httpd:latest
            command:
            - /bin/sh
            - -c
            - echo Welcome to companyname!